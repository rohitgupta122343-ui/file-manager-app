
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

</head>
<body>
   
<div class="w-full min-h-screen bg-zinc-900 text-white p-10">

    <form method="post" action="/create">
        <input class="px-3 py-2 bg-transparent border-2 border-zinc-800 rounded-md block mb-2 outline-none" type="text" placeholder="Title.." name="title">
        <textarea class="bg-transparent border-2 border-zinc-800 rounded-md resize-none w-1/3 px-3 py-2 outline-none block" placeholder="details..." name="content"></textarea>
        <input class="px-3 py-2 bg-blue-500 rounded-md mt-2" type="submit" value="Create a task">
    </form>

    <div class="tasks mt-15 flex gap-2">

     <% if (files.length > 0) { %>
    <% files.forEach(function(val){ %>
        <div class="task border-2 bg-transparent border-zinc-800 px-3 py-2 rounded-md w-1/3">
            <h4 class="text-3xl"><%= val %></h4>
            <div class="flex justify-between mt-3">
                <a class="text-blue-500" href=" read/<%= val  %> ">Read more</a>
                <a class="text-zinc-500" href="edit/<%= val %>">Edit Filename</a>
            </div>
        </div>
    <% }) %>
<% } else { %>
    <p class="text-zinc-400 text-lg mt-5">No files found.</p>
<% } %>


    </div>

</div>

</body>
</html>